# TODO — Academic portfolio

- Add real images: `/assets/profile.jpeg` and `/assets/institution-logo.png` (recommended sizes: 400x400 and 120x40).
- Improve SEO: add meta description, structured data (JSON-LD).
- Add printable CV download link and a separate `cv.pdf` file.
- Add pagination or filtering for long publication lists.
- Add unit tests for renderer functions.
- Add light build step for Tailwind instead of CDN for production.
- Optionally add internationalization support (i18n).
